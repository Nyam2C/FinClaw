# CLAUDE.md

일반적인 LLM 코딩 실수를 줄이기 위한 행동 가이드라인. 프로젝트별 지침과 병합하여 사용할 것.

**트레이드오프:** 이 가이드라인은 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단력을 발휘할 것.

## 1. 코딩 전에 생각하기

**추측하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전:

- 가정을 명시적으로 밝힐 것. 불확실하면 질문할 것.
- 여러 해석이 가능하면 제시할 것 -- 조용히 하나를 고르지 말 것.
- 더 단순한 접근법이 있다면 말할 것. 필요하면 반론을 제기할 것.
- 무언가 불분명하면 멈출 것. 무엇이 혼란스러운지 명시하고 질문할 것.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드. 추측성 코드는 없을 것.**

- 요청받지 않은 기능은 추가하지 말 것.
- 한 번만 쓰는 코드에 추상화를 만들지 말 것.
- 요청되지 않은 "유연성"이나 "설정 가능성"을 넣지 말 것.
- 일어날 수 없는 시나리오에 대한 에러 처리를 하지 말 것.
- 200줄로 작성했는데 50줄이면 충분하다면 다시 작성할 것.

스스로에게 물어볼 것: "시니어 엔지니어가 이것을 과도하게 복잡하다고 할까?" 그렇다면 단순화할 것.

## 3. 외과적 변경

**필요한 것만 수정할 것. 자신이 만든 문제만 정리할 것.**

기존 코드를 수정할 때:

- 인접한 코드, 주석, 포매팅을 "개선"하지 말 것.
- 고장나지 않은 것을 리팩토링하지 말 것.
- 기존 스타일에 맞출 것, 자신이 다르게 할 수 있더라도.
- 관련 없는 죽은 코드를 발견하면 언급할 것 -- 삭제하지 말 것.

자신의 변경이 고아 코드를 만들 때:

- 자신의 변경으로 인해 미사용이 된 import/변수/함수는 제거할 것.
- 요청받지 않은 한 기존의 죽은 코드는 제거하지 말 것.

테스트 기준: 변경된 모든 줄은 사용자의 요청에 직접 연결되어야 한다.

## 4. 목표 기반 실행

**성공 기준을 정의할 것. 검증될 때까지 반복할 것.**

작업을 검증 가능한 목표로 변환할 것:

- "검증 추가" → "잘못된 입력에 대한 테스트를 작성하고 통과시키기"
- "버그 수정" → "재현하는 테스트를 작성하고 통과시키기"
- "X 리팩토링" → "전후로 테스트가 통과하는지 확인하기"

다단계 작업에는 간략한 계획을 명시할 것:

```
1. [단계] → 검증: [확인 사항]
2. [단계] → 검증: [확인 사항]
3. [단계] → 검증: [확인 사항]
```

강한 성공 기준은 독립적인 반복을 가능하게 한다. 약한 기준("되게 해줘")은 끊임없는 확인이 필요하다.

---

**이 가이드라인이 작동하고 있다면:** diff에 불필요한 변경이 줄고, 과도한 복잡성으로 인한 재작성이 줄고, 실수 후가 아니라 구현 전에 명확화 질문이 나온다.

---

## 하네스: Phase 26 — 기억 & 거래 시스템

**목표:** FinClaw 가 사용자의 거래·기억·과거 분석을 영속화하고, 새 대화에서 자동 회상하여 system prompt 에 주입하는 3축(거래·기억·RAG) 완성.

**트리거:** 다음 키워드 또는 작업 요청 시 `phase26-orchestrator` 스킬을 사용하라.

- "Phase 26", "기억 시스템", "기억·거래", "transactions 추가", "RAG 주입", "agent.run 저장", "feature/memory-and-transactions" 브랜치 작업
- 후속: "다시 실행", "재실행", "이어서", "밀스톤 X 만 다시", "이전 결과 기반", "보완", "수정"
- 단순 질문(예: "transactions 테이블 컬럼이 뭐였지")은 직접 응답 가능.

**변경 이력:**

| 날짜       | 변경 내용                       | 대상 | 사유                                |
| ---------- | ------------------------------- | ---- | ----------------------------------- |
| 2026-04-28 | 초기 구성 (6 에이전트 + 6 스킬) | 전체 | Phase 26 비서 기억 시스템 구현 착수 |

---

## 하네스: 현대 AI 비서 성숙도 감사

**목표:** FinClaw 의 구현 규모·아키텍처가 현대 AI 비서(Claude.ai, ChatGPT, Letta/MemGPT, Hermes 류, MCP 표준) 수준에 도달했는지 종합 평가하고 갭을 우선순위로 보고. 사용자 1인·직접 학습 비대상 제약 반영.

**트리거:** 다음 키워드 또는 작업 요청 시 `finclaw-maturity-audit` 스킬을 사용하라.

- "성숙도 감사", "현대 AI 비서 수준 평가", "OpenClaw/헤르메스 비교", "갭 분석", "아키텍처 감사", "에이전트 시스템 평가"
- 후속: "다시 감사", "재평가", "이어서 감사", "특정 영역만 다시", "이전 감사 기반 보완", "보고서 업데이트"
- 단순 질문(예: "패키지가 몇 개야") 은 직접 응답.

**변경 이력:**

| 날짜       | 변경 내용                                             | 대상                   | 사유                                                                                            |
| ---------- | ----------------------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------- |
| 2026-05-03 | 초기 구성 (5 에이전트 + 1 오케스트레이터 스킬 + 룰릭) | .claude/agents, skills | "현대 AI 비서급 성숙도 감사" 요청에 따라 architecture/runtime/memory/interface 4축 감사 팀 구성 |

---

## 하네스: README 작성 / 갱신

**목표:** FinClaw README.md 를 코드 진실에 고정하여 작성·갱신. 4 explorer(positioning / features / architecture / ops) 병렬 수집 → author 합성 → verifier 사실 검증 → 사용자 승인 후 README.md 반영.

**트리거:** 다음 키워드 또는 작업 요청 시 `readme-orchestrator` 스킬을 사용하라.

- "README 작성", "README 갱신", "README 다시 만들어", "리드미", "프로젝트 소개 문서", "README 의 X 섹션만 다시"
- 후속: "재작성", "이전 README 기반 보완", "수정", "다시 실행"
- 단순한 한 줄 수정(예: 오타)은 직접 편집.

**변경 이력:**

| 날짜       | 변경 내용                       | 대상                   | 사유                                                             |
| ---------- | ------------------------------- | ---------------------- | ---------------------------------------------------------------- |
| 2026-05-03 | 초기 구성 (6 에이전트 + 3 스킬) | .claude/agents, skills | "프로젝트 정체와 기능을 외부 독자에게 전달하는 README 부재" 요청 |

---

## 하네스: FinClaw ↔ OpenClaw 유사도 비교

**목표:** FinClaw 가 OpenClaw(`/mnt/c/Users/박/Desktop/hi/openclaw`) 의 핵심 패턴을 얼마나 충실히 재현했는지를 4축(아키텍처/런타임/메모리/인터페이스)으로 1:1 매핑 비교(라벨 + 유사도 %)하여 단일 종합 보고서로 답한다. 기존 `finclaw-maturity-audit`(현대 AI 비서 일반 비교, 점수 0-5) 와 트리거·평가축이 분리됨.

**트리거:** 다음 키워드 또는 작업 요청 시 `finclaw-openclaw-similarity` 스킬을 사용하라.

- "OpenClaw 비교", "유사도 검토", "모방 충실도", "OpenClaw 와 얼마나 닮았나", "두 레포 비교", "OpenClaw 잘 따라했나", "정당한 단순화 vs 누락", "금융 도메인 합체 품질"
- 후속: "다시 비교", "재비교", "이어서 비교", "특정 영역만 다시 비교", "이전 비교 기반 보완", "비교 보고서 업데이트", "{축} 비교만 다시"
- 단순 사실 질문(예: "OpenClaw 의 src 안에 뭐가 있어") 은 직접 응답.

**변경 이력:**

| 날짜       | 변경 내용                                                                                                   | 대상                           | 사유                                                                                                                                      |
| ---------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-03 | 초기 구성 (1 오케스트레이터 스킬 + 2 references + 4 auditor 모드 분기 추가; 기존 4 auditor 에이전트 재사용) | .claude/skills, .claude/agents | "FinClaw 가 OpenClaw 의 핵심 패턴을 충실히 모방했는지 1:1 매핑으로 검증" 요청. 기존 maturity-audit 와 평가 형식이 달라 별도 하네스로 분리 |
